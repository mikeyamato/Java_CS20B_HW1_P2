package exercise2;

public class BiasedDicePair {
  /*
  import `java.util.Random` and limit the range to be within the range you want the die
  to be weighted.

  Random r = new Random();
	return r.nextInt((max - min) + 1) + min;
   */
}
